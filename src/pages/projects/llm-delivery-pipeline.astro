---
import Layout from '../../layouts/Layout.astro';
import { getEntry, render } from 'astro:content';

// The slug is hardcoded here and must match the content filename
// (src/content/projects/llm-delivery-pipeline.md) — see CLAUDE.md.
const entry = await getEntry('projects', 'llm-delivery-pipeline');
const { Content } = await render(entry);
---

<Layout
  title={`${entry.data.title} — Jesús Caballero Rodríguez`}
  description={entry.data.tagline}
  editSlug="projects/llm-delivery-pipeline"
>
  <main class="page page-doc">
    <Content />
  </main>
</Layout>
