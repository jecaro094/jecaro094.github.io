---
import Layout from '../../layouts/Layout.astro';
import { getEntry, render } from 'astro:content';

// The slug is hardcoded here and must match the content filename
// (src/content/projects/qr-voting-survey.md) — see CLAUDE.md.
const entry = await getEntry('projects', 'qr-voting-survey');
const { Content } = await render(entry);
---

<Layout
  title={`${entry.data.title} — Jesús Caballero Rodríguez`}
  description={entry.data.tagline}
  editSlug="projects/qr-voting-survey"
>
  <main class="page page-doc">
    <Content />
  </main>
</Layout>
